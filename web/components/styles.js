import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    paddingLeft: 15,
    paddingRight: 15,
    width: "auto",
    height: "100%",
  },
  header: {
    fontSize: 40,
    marginTop: 15,
    marginBottom: 60,
    color: "black",
  },
  input: { width: "70%", marginBottom: 20 },
});

export { styles };
